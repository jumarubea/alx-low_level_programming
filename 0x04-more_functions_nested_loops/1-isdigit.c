#include "main.h"

/**
 * _isdigit - to print a digit
 *
 * Description: a fuctions
 * @c: a character
 *
 * Return: a 1 if true and 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= 0 && c <= 31);
}
